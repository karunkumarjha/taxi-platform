# Streamlit-in-Snowflake conda environment.
# Snowflake's `snowflake` channel is required — third-party PyPI packages
# are not allowed in SiS.
name: app_environment
channels:
  - snowflake
dependencies:
  - streamlit=1.39.0
  - snowflake-snowpark-python=1.27.0
  - plotly=5.24.1
  - pandas=2.2.3
