# Astro Runtime ships with Airflow + common providers pre-installed.
# We add: Snowflake provider, dbt + adapter, AWS SDK, HTTP client, retry helper.
#
# All deps pinned EXACTLY — loose ranges trigger pip backtracking hell on
# dbt-adapters resolution (every dbt-snowflake patch combinatorially explodes
# against every dbt-adapters version). Lockstep upgrade in one PR if needed.

apache-airflow-providers-snowflake==5.8.0
apache-airflow-providers-amazon==9.1.0

# typer 0.18+ removed the [all] extra. dbt's deps still request typer[all],
# which sends pip into multi-minute backtracking through every typer/dbt-*
# version combination. Pinning typer to the last version with [all] cuts
# the resolver dead.
typer==0.12.5

# Astro Runtime 12.1.1 ships opentelemetry-proto 1.27.0 which pins
# protobuf<5. dbt 1.8+ requires protobuf>=5 (and 1.9+ requires protobuf>=6).
# Pinning dbt to 1.7.x stays compatible. All features we use (constraints,
# custom tests, sources/freshness, dbt_utils, dbt_expectations) work in 1.7.
dbt-core==1.7.18
dbt-snowflake==1.7.5

boto3==1.40.62
requests==2.32.5
tenacity==9.1.4
snowflake-connector-python==3.18.0
